def main():
    while True:
        sales = float(input("Enter sales: $"))
        if sales < 1000:
            bonus1 = sales * 0.1
            print("Your bonus is", bonus1)
        elif sales >= 1000:
            bonus2 = sales * 0.15
            print("Your bonus is", bonus2)


main()